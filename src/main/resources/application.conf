    ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.example.customer.ApplicationKt.module ]
        modules = [ com.example.movies.ApplicationKt.module ]
        modules = [ com.example.pages.ApplicationKt.module ]
    }
}
